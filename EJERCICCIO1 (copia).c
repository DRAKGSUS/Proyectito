#include <stdio.h>

int main() {
   int a = 2;
   int b = 3;
   int c = 4;
   int resultado = a * b * c;
   printf("El resultado de la multiplicación de %d, %d y %d es %d", a, b, c, resultado);
   return 0;
}